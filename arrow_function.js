// single line hole second backet use kora lage na
const add=(n1)=>n1+5;
console.log(add(5));
// multiple line hole second bracket use kora lage
const doMath=(x,y)=>{
    const add=x+y;
    const sub=x-y;
    const result=add*sub;
    return result;
}
console.log(doMath(8,4));